 import scala.collection.mutable.{Map}

class DoubleTransposition(val r: Int, val c: Int, val k: Map[String, Array[Array[Int]]]) {
  var rows: Int = r
  var cols: Int = c
  var key: Map[String, Array[Array[Int]]] = k

  def encrypt(plaintext: String) {
    
  }
}

object Run {
  def main(agrs: Array[String]) {
    println("Hello, world!")
  }
}